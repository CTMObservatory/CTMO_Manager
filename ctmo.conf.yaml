Scheduler Address: {
  HTTP: "http://localhost:8000/",
  IP: localhost,
  Port: 8000
}

Telescope Address: {
  HTTP: "http://localhost:8010/",
  IP: localhost,
  Port: 8010
}

Logging: {
  File: /etc/ctmo/logs/ctmo.log,
  Log Level: INFO
}
